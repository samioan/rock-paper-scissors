import {
  setPlayerName,
  setPlayerSelection,
  setPlayerPending,
  setPlayerResult,
} from "./actions";

import {
  playerName,
  playerWins,
  playerLosses,
  playerDraws,
  playerSelection,
  playerPending,
  playerResult,
} from "./selectors";

export {
  setPlayerName,
  setPlayerSelection,
  setPlayerPending,
  setPlayerResult,
  playerName,
  playerWins,
  playerLosses,
  playerDraws,
  playerSelection,
  playerPending,
  playerResult,
};
